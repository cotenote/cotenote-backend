dependencies {
    // Spring Data JPA
    api("org.springframework.boot:spring-boot-starter-data-jpa")

    // MySQL Driver
    runtimeOnly("com.mysql:mysql-connector-j")
}

bootJar {
    enabled = false
}
jar {
    enabled = true
}
